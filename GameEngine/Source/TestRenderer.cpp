
#include "TestRenderer.h"

TestRenderer::TestRenderer(sf::Shape *shape) {
	this->shape = shape;
}

/*
void TestRenderer::draw(sf::RenderTarget & target, sf::RenderStates states) {
	printf("Called from test renderer draw");
	target.draw(*shape);
}
*/
/*
void TestRenderer::draw(sf::RenderTarget& target, sf::RenderStates states) {
	target.draw(*shape);
}
*/